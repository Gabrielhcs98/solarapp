<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.SolarApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.SolarApp" parent="Base.Theme.SolarApp" />

    <!-- Full-screen theme for MainActivity -->
    <style name="FullScreenTheme" parent="Base.Theme.SolarApp">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
